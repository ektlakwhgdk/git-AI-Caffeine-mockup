# 회원가입 기능 가이드

## 📝 개요
AI Caffeine Advisor의 회원가입 기능은 사용자 정보를 수집하여 개인화된 카페인 관리 서비스를 제공합니다.

## 🎯 회원가입 필드

### 1. **아이디** (필수)
- **타입**: 텍스트
- **최소 길이**: 4자
- **유효성 검사**: 4자 이상 입력 필수
- **에러 메시지**: "아이디는 4자 이상이어야 합니다."

```typescript
id: string; // 최소 4자
```

### 2. **비밀번호** (필수)
- **타입**: 패스워드
- **최소 길이**: 6자
- **유효성 검사**: 6자 이상 입력 필수
- **에러 메시지**: "비밀번호는 6자 이상이어야 합니다."

```typescript
password: string; // 최소 6자
```

### 3. **이름** (필수)
- **타입**: 텍스트
- **최소 길이**: 2자
- **유효성 검사**: 2자 이상 입력 필수
- **에러 메시지**: "이름을 올바르게 입력해주세요."

```typescript
name: string; // 최소 2자
```

### 4. **생년월일** (필수)
- **타입**: 날짜 (date)
- **형식**: YYYY-MM-DD
- **유효성 검사**: 
  - 날짜 선택 필수
  - 만 14세 이상만 가입 가능
- **에러 메시지**: 
  - "생년월일을 선택해주세요."
  - "만 14세 이상부터 가입 가능합니다."

```typescript
birthDate: string; // ISO 날짜 형식
```

### 5. **성별** (필수)
- **타입**: 선택 (Select)
- **옵션**: 
  - 남자 (`male`)
  - 여자 (`female`)
- **유효성 검사**: 성별 선택 필수
- **에러 메시지**: "성별을 선택해주세요."

```typescript
gender: "male" | "female" | "";
```

## 🔐 유효성 검사 로직

### 검사 순서
1. 아이디 길이 확인 (4자 이상)
2. 비밀번호 길이 확인 (6자 이상)
3. 이름 길이 확인 (2자 이상)
4. 생년월일 입력 확인
5. 성별 선택 확인
6. 나이 계산 및 확인 (만 14세 이상)

### 코드 예시
```typescript
const handleSignup = (e: React.FormEvent) => {
  e.preventDefault();
  
  // 아이디 검사
  if (!signupForm.id || signupForm.id.length < 4) {
    toast.error("아이디는 4자 이상이어야 합니다.");
    return;
  }
  
  // 비밀번호 검사
  if (!signupForm.password || signupForm.password.length < 6) {
    toast.error("비밀번호는 6자 이상이어야 합니다.");
    return;
  }
  
  // 이름 검사
  if (!signupForm.name || signupForm.name.length < 2) {
    toast.error("이름을 올바르게 입력해주세요.");
    return;
  }
  
  // 생년월일 검사
  if (!signupForm.birthDate) {
    toast.error("생년월일을 선택해주세요.");
    return;
  }
  
  // 성별 검사
  if (!signupForm.gender) {
    toast.error("성별을 선택해주세요.");
    return;
  }
  
  // 나이 계산 및 검사
  const birthYear = new Date(signupForm.birthDate).getFullYear();
  const currentYear = new Date().getFullYear();
  const age = currentYear - birthYear;
  
  if (age < 14) {
    toast.error("만 14세 이상부터 가입 가능합니다.");
    return;
  }
  
  // 회원가입 성공 처리
  // ...
};
```

## 💾 데이터 저장

### localStorage 저장
회원가입 성공 시 사용자 정보를 `localStorage`에 저장합니다.

```typescript
const userData = {
  ...signupForm,
  age,
  createdAt: new Date().toISOString()
};

localStorage.setItem("user_profile", JSON.stringify(userData));
```

### 저장되는 데이터 구조
```json
{
  "id": "user123",
  "password": "******",
  "name": "홍길동",
  "birthDate": "1990-01-01",
  "gender": "male",
  "age": 35,
  "createdAt": "2025-11-07T14:00:00.000Z"
}
```

## 🎨 UI/UX

### 폼 레이아웃
- **다이얼로그 형식**: 모달 팝업으로 표시
- **최대 너비**: 모바일 90%, 데스크톱 md (448px)
- **둥근 모서리**: 24px (rounded-3xl)
- **간격**: 각 필드 간 20px (space-y-5)

### 입력 필드 스타일
- **높이**: 48px (h-12)
- **둥근 모서리**: 12px (rounded-xl)
- **포커스 효과**: 
  - 테두리 색상 변경
  - Ring 효과 (ring-2)
  - 부드러운 전환 (transition-all)

### 버튼 스타일
- **가입하기 버튼**:
  - 전체 너비 (w-full)
  - 높이 48px (h-12)
  - Primary 색상
  - 호버 시 스케일 효과 (hover:scale-105)

## 📱 사용자 경험

### 성공 시나리오
1. 사용자가 모든 필드를 올바르게 입력
2. 유효성 검사 통과
3. 성공 토스트 메시지 표시
   ```
   제목: "회원가입이 완료되었습니다!"
   설명: "{이름}님, 환영합니다!"
   ```
4. 다이얼로그 닫힘
5. 메인 화면으로 이동

### 실패 시나리오
1. 사용자가 필드를 잘못 입력
2. 유효성 검사 실패
3. 에러 토스트 메시지 표시
4. 해당 필드에 포커스 유지
5. 사용자가 수정 후 재시도

## 🔧 타입 정의

### SignupForm 인터페이스
```typescript
export interface SignupForm {
  id: string;
  password: string;
  name: string;
  birthDate: string;
  gender: "male" | "female" | "";
}
```

### UserProfile 인터페이스
```typescript
export interface UserProfile {
  id: string;
  name: string;
  email?: string;
  avatar?: string;
  dailyLimit: number;
  weight?: number;
  height?: number;
  birthDate?: Date;
  gender?: "male" | "female";
  preferences: {
    notifications: boolean;
    darkMode: boolean;
    language: string;
  };
}
```

## 🚀 향후 개선 사항

### 우선순위 높음
- [ ] 아이디 중복 확인
- [ ] 비밀번호 확인 필드 추가
- [ ] 비밀번호 강도 표시
- [ ] 이메일 인증

### 우선순위 중간
- [ ] 소셜 로그인 (Google, Kakao, Naver)
- [ ] 프로필 사진 업로드
- [ ] 약관 동의 체크박스
- [ ] 개인정보 처리방침

### 우선순위 낮음
- [ ] 추천인 코드
- [ ] 마케팅 수신 동의
- [ ] 다국어 지원

## 📊 접근성 (Accessibility)

### 구현된 기능
- ✅ 모든 입력 필드에 `Label` 연결
- ✅ `required` 속성으로 필수 필드 표시
- ✅ 명확한 에러 메시지
- ✅ 키보드 네비게이션 지원

### 개선 필요 사항
- [ ] ARIA 레이블 추가
- [ ] 스크린 리더 지원 강화
- [ ] 에러 필드 자동 포커스

## 🧪 테스트 시나리오

### 정상 케이스
```typescript
{
  id: "testuser",
  password: "test1234",
  name: "테스트",
  birthDate: "2000-01-01",
  gender: "male"
}
// 예상 결과: 회원가입 성공
```

### 에러 케이스
```typescript
// 케이스 1: 짧은 아이디
{ id: "abc", ... }
// 예상 결과: "아이디는 4자 이상이어야 합니다."

// 케이스 2: 짧은 비밀번호
{ password: "12345", ... }
// 예상 결과: "비밀번호는 6자 이상이어야 합니다."

// 케이스 3: 미성년자
{ birthDate: "2015-01-01", ... }
// 예상 결과: "만 14세 이상부터 가입 가능합니다."

// 케이스 4: 성별 미선택
{ gender: "", ... }
// 예상 결과: "성별을 선택해주세요."
```

## 📚 관련 파일

- **컴포넌트**: `src/screens/OnboardingScreen.tsx`
- **타입 정의**: `src/types/index.ts`
- **스타일**: `src/styles/signup.module.css`
- **문서**: `SIGNUP_GUIDE.md`

---

**마지막 업데이트**: 2025년 11월 7일
